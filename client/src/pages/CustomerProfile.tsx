import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { trpc } from "@/lib/trpc";
import { customerMapUrl, formatDateTime, reminderStatusLabels, toDateTimeLocal, visitTypeLabels } from "@/lib/filterUi";
import { ArrowRight, BellRing, CalendarPlus, Loader2, MapPinned, Phone, Plus } from "lucide-react";
import { FormEvent, useMemo, useState } from "react";
import { toast } from "sonner";
import { useLocation, useRoute } from "wouter";

export default function CustomerProfile() {
  const [, params] = useRoute("/customers/:id");
  const customerId = Number(params?.id);
  const queryInput = useMemo(() => ({ id: customerId }), [customerId]);
  const { data, isLoading } = trpc.filters.customers.get.useQuery(queryInput, { enabled: Number.isFinite(customerId) });
  const [dialogOpen, setDialogOpen] = useState(false);
  const [visitType, setVisitType] = useState<keyof typeof visitTypeLabels>("maintenance");
  const [visitDate, setVisitDate] = useState(toDateTimeLocal());
  const [notes, setNotes] = useState("");
  const [, setLocation] = useLocation();
  const utils = trpc.useUtils();
  const createVisit = trpc.filters.visits.create.useMutation({ onSuccess: result => { utils.filters.customers.get.invalidate(queryInput); utils.filters.dashboard.invalidate(); utils.filters.reminders.due.invalidate(); toast.success(result.reminderCreated ? "تم تسجيل الزيارة وإنشاء تذكير بعد 120 يومًا" : "تم تسجيل الزيارة بنجاح"); setDialogOpen(false); setNotes(""); }, onError: error => toast.error(error.message || "تعذر تسجيل الزيارة. يرجى المحاولة مرة أخرى.") });
  if (isLoading) return <div className="grid min-h-72 place-items-center"><Loader2 className="h-6 w-6 animate-spin text-teal-700" /></div>;
  if (!data) return <div className="soft-card p-8 text-center"><p className="font-bold">تعذر العثور على العميل.</p><Button onClick={() => setLocation("/customers")} variant="outline" className="mt-4 rounded-xl">العودة للعملاء</Button></div>;
  const { customer } = data;
  const mapUrl = customerMapUrl(customer);
  function submitVisit(event: FormEvent) { event.preventDefault(); createVisit.mutate({ customerId, visitType, visitDate: new Date(visitDate), notes: notes || null }); }
  return <div className="mx-auto max-w-6xl space-y-6"><button onClick={() => setLocation("/customers")} className="inline-flex items-center text-sm font-bold text-teal-700"><ArrowRight className="ml-1 h-4 w-4" />العودة إلى العملاء</button><section className="soft-card overflow-hidden"><div className="bg-[linear-gradient(135deg,#064e4a,#0f766e)] p-6 text-white sm:flex sm:items-center sm:justify-between"><div><p className="text-sm text-teal-100">ملف العميل</p><h1 className="mt-1 text-2xl font-extrabold">{customer.name}</h1><p className="mt-2 text-sm text-teal-50/80" dir="ltr">{customer.phone}</p></div><Button onClick={() => setDialogOpen(true)} className="mt-5 rounded-xl bg-white text-teal-800 hover:bg-teal-50 sm:mt-0"><CalendarPlus className="ml-2 h-4 w-4" />تسجيل زيارة</Button></div><div className="grid gap-4 p-6 sm:grid-cols-3"><div><p className="text-xs font-bold text-muted-foreground">العنوان</p><p className="mt-2 text-sm leading-6">{customer.address || "غير مسجل"}</p></div><div><p className="text-xs font-bold text-muted-foreground">الموقع GPS</p><p className="mt-2 text-sm" dir="ltr">{customer.latitude && customer.longitude ? `${customer.latitude}, ${customer.longitude}` : "غير مسجل"}</p></div><div className="flex items-end gap-2"><a href={`tel:${customer.phone}`} className="inline-flex h-10 items-center rounded-xl bg-teal-50 px-3 text-sm font-bold text-teal-800 hover:bg-teal-100"><Phone className="ml-2 h-4 w-4" />اتصال</a>{mapUrl ? <a href={mapUrl} target="_blank" rel="noreferrer" className="inline-flex h-10 items-center rounded-xl bg-sky-50 px-3 text-sm font-bold text-sky-800 hover:bg-sky-100"><MapPinned className="ml-2 h-4 w-4" />الخريطة</a> : null}</div></div>{customer.notes ? <div className="border-t border-teal-950/6 bg-teal-50/35 px-6 py-4 text-sm leading-7 text-muted-foreground"><span className="font-bold text-teal-950">ملاحظات: </span>{customer.notes}</div> : null}</section><section className="grid gap-6 lg:grid-cols-3"><div className="soft-card lg:col-span-2"><div className="flex items-center justify-between border-b border-teal-950/6 p-5"><div><h2 className="font-extrabold">سجل الزيارات</h2><p className="mt-1 text-xs text-muted-foreground">جميع الزيارات المسجلة للعميل</p></div><span className="rounded-full bg-teal-50 px-3 py-1 text-xs font-bold text-teal-800">{data.visits.length} زيارة</span></div><div className="divide-y divide-teal-950/6">{data.visits.length ? data.visits.map(visit => <div key={visit.id} className="flex gap-4 p-5"><div className="mt-1 h-3 w-3 shrink-0 rounded-full bg-teal-500 ring-4 ring-teal-50" /><div className="min-w-0 flex-1"><div className="flex flex-wrap items-center justify-between gap-2"><p className="font-bold">{visitTypeLabels[visit.visitType]}</p><p className="text-xs text-muted-foreground">{formatDateTime(visit.visitDate)}</p></div>{visit.notes ? <p className="mt-2 text-sm leading-6 text-muted-foreground">{visit.notes}</p> : null}</div></div>) : <div className="p-10 text-center text-sm text-muted-foreground">لا توجد زيارات سابقة لهذا العميل.</div>}</div></div><div className="soft-card"><div className="flex items-center gap-2 border-b border-teal-950/6 p-5"><BellRing className="h-5 w-5 text-amber-500" /><div><h2 className="font-extrabold">التذكيرات</h2><p className="mt-1 text-xs text-muted-foreground">متابعة الصيانة</p></div></div><div className="divide-y divide-teal-950/6">{data.reminders.length ? data.reminders.map(reminder => <div key={reminder.id} className="p-4"><Badge variant="secondary" className={reminder.status === "pending" ? "bg-amber-100 text-amber-800" : "bg-teal-100 text-teal-800"}>{reminderStatusLabels[reminder.status]}</Badge><p className="mt-2 text-sm font-bold">{formatDateTime(reminder.reminderDate)}</p></div>) : <div className="p-8 text-center text-sm text-muted-foreground">لا يوجد تذكير مسجل.</div>}</div></div></section><Dialog open={dialogOpen} onOpenChange={setDialogOpen}><DialogContent dir="rtl"><DialogHeader><DialogTitle>تسجيل زيارة للعميل</DialogTitle></DialogHeader><form onSubmit={submitVisit} className="space-y-4 py-2"><label><span className="field-label">نوع الزيارة</span><select className="field-input" value={visitType} onChange={event => setVisitType(event.target.value as keyof typeof visitTypeLabels)}>{Object.entries(visitTypeLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label><span className="field-label">تاريخ ووقت الزيارة</span><input type="datetime-local" className="field-input" value={visitDate} onChange={event => setVisitDate(event.target.value)} required /></label><label><span className="field-label">ملاحظات الزيارة</span><textarea className="field-textarea" value={notes} onChange={event => setNotes(event.target.value)} placeholder="اكتب تفاصيل مختصرة عن الخدمة" /></label><div className="flex justify-end gap-3"><Button type="button" variant="outline" onClick={() => setDialogOpen(false)} className="rounded-xl">إلغاء</Button><Button type="submit" disabled={createVisit.isPending} className="rounded-xl bg-teal-700 hover:bg-teal-800">{createVisit.isPending ? "جارٍ التسجيل…" : "حفظ الزيارة"}</Button></div></form></DialogContent></Dialog></div>;
}
