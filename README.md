# نقطة نقاء — Render deployment

هذا المستودع يحتوي على أرشيف المصدر النظيف وملف Render Blueprint. يقوم أمر البناء بفك الأرشيف ثم تثبيت الاعتمادات وبناء التطبيق.

يجب إضافة DATABASE_URL وJWT_SECRET من لوحة Render قبل أول نشر. لا توجد أسرار داخل المستودع.
